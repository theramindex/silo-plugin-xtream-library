# Local Wayfinder tracker

This repository has no configured remote issue tracker yet. These Markdown
files are the local tracker until the project moves to its destination
repository. Every issue records its label, parent map, and blockers in YAML
front matter; the map is the index.
